"""Plotting helpers — save PNGs to outputs/.

TODO (Phase 2):
- plot_decomposition(result, title) -> saves decomposition figure.
- plot_dow_pattern(series) -> day-of-week bar chart.
- plot_hour_pattern(events) -> hour-of-day bar chart.
- plot_duration_distribution(events) -> histogram.
- plot_forecast(actuals, forecast, title) -> forecast vs actual with PI shading.

All functions should accept an optional `output_dir: Path` defaulting to
config.OUTPUTS_DIR and save figures there (no plt.show() in pipeline mode).
"""

# TODO: implement plotting helpers.